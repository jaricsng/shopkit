# Prometheus recording rules — generic SLI/SLO definitions for an
# OTel-instrumented HTTP service.
#
# SLOs (adjust thresholds to your own targets):
#   Latency      — P95 response time < 500 ms
#   Errors       — 5xx error rate < 1%
#   Availability — /ready probe success > 99.9% over a rolling window
#
# Metric source: OTel STABLE HTTP semantic conventions (1.23+)
#   http.server.request.duration → http_server_request_duration_seconds (histogram, in SECONDS)
#   Labels: http_request_method, http_response_status_code, http_route, url_scheme
#
# The reference resolves the kit's documented old-vs-new metric-name mismatch
# toward the STABLE seconds-based name (the app opts in via
# OTEL_SEMCONV_STABILITY_OPT_IN=http) so these rules, the Grafana dashboard, and
# the app all agree. See observability/METRIC-NAME-DECISION.md.

groups:
  - name: app-sli
    interval: 60s
    rules:
      # ── Request rate ─────────────────────────────────────────────────────────
      - record: job:http_requests:rate5m
        expr: |
          sum(rate(http_server_request_duration_seconds_count[5m]))
        labels:
          job: app

      # ── Error rate (5xx) ─────────────────────────────────────────────────────
      - record: job:http_errors_5xx:rate5m
        expr: |
          sum(rate(http_server_request_duration_seconds_count{http_response_status_code=~"5.."}[5m]))
        labels:
          job: app

      # SLI: error ratio (0.0–1.0). SLO threshold: < 0.01 (1%)
      - record: job:http_error_ratio:rate5m
        expr: |
          job:http_errors_5xx:rate5m
          /
          job:http_requests:rate5m
        labels:
          job: app

      # ── Latency ──────────────────────────────────────────────────────────────
      # SLI: P95 latency in milliseconds (×1000 from the seconds-based histogram).
      # SLO threshold: < 500 ms
      - record: job:http_p95_latency_ms:rate5m
        expr: |
          histogram_quantile(0.95,
            sum(rate(http_server_request_duration_seconds_bucket[5m])) by (le)
          ) * 1000
        labels:
          job: app

      # P99 latency for observability (not an SLO target, just tracked)
      - record: job:http_p99_latency_ms:rate5m
        expr: |
          histogram_quantile(0.99,
            sum(rate(http_server_request_duration_seconds_bucket[5m])) by (le)
          ) * 1000
        labels:
          job: app

      # ── Availability ─────────────────────────────────────────────────────────
      # SLI: fraction of time /ready probe succeeds. SLO target: > 0.999 (99.9%)
      - record: job:readiness_probe_success:avg5m
        expr: |
          avg_over_time(probe_success{job="readiness"}[5m])
        labels:
          job: app

      # ── Rejection rate (429) ─────────────────────────────────────────────────
      # Rate-limiter firing — elevated values can indicate brute-force attempts
      - record: job:http_rejections:rate1m
        expr: |
          sum(rate(http_server_request_duration_seconds_count{http_response_status_code="429"}[1m]))
        labels:
          job: app

      # ── Error budget naming (alias) ──────────────────────────────────────────
      # Some teams prefer a `<service>:` prefix convention for SLO dashboards.
      # Rename "app" below if you want a service-specific alias; both sets of
      # names resolve to the same values.

      # Availability SLI: ratio of successful (non-5xx) requests
      - record: app:request_availability:rate5m
        expr: |
          1 - (
            sum(rate(http_server_request_duration_seconds_count{http_response_status_code=~"5.."}[5m]))
            /
            sum(rate(http_server_request_duration_seconds_count[5m]))
          )

      # Latency SLI: P95 request duration in seconds (metric is already seconds)
      - record: app:request_p95_seconds:rate5m
        expr: |
          histogram_quantile(0.95,
            sum(rate(http_server_request_duration_seconds_bucket[5m])) by (le)
          )

      # Error budget burn rate (30-day window, 1-hour burn rate)
      # Burn rate > 1.0 means on track to exhaust budget in < 30 days
      # Burn rate > 14.4 means budget gone in < 2 hours — page immediately
      - record: app:error_budget_burn_rate:1h
        expr: |
          (
            sum(rate(http_server_request_duration_seconds_count{http_response_status_code=~"5.."}[1h]))
            /
            sum(rate(http_server_request_duration_seconds_count[1h]))
          ) / 0.01

  - name: app-slo
    rules:
      # Page immediately if burn rate is so high the budget will be gone in 2 hours
      - alert: ErrorBudgetBurnRateCritical
        expr: app:error_budget_burn_rate:1h > 14.4
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "SLO error budget burning at critical rate"
          description: >
            Burn rate {{ $value | humanize }}x — at this rate the 30-day error budget
            (1% of requests) will be exhausted in less than 2 hours.

      # Warn if burn rate predicts budget exhaustion in < 6 hours
      - alert: ErrorBudgetBurnRateHigh
        expr: app:error_budget_burn_rate:1h > 6
        for: 15m
        labels:
          severity: warning
        annotations:
          summary: "SLO error budget burning at elevated rate"
          description: >
            Burn rate {{ $value | humanize }}x — budget exhaustion predicted in < 6 hours.

      # Latency SLO breach — P95 > 500 ms (0.5 s)
      - alert: LatencySLOBreach
        expr: app:request_p95_seconds:rate5m > 0.5
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "P95 latency exceeds 500 ms SLO"
          description: "Current P95: {{ $value | humanizeDuration }}"

      # Readiness SLO breach
      # TODO: update the instance URL to match your service's hostname:port
      - alert: ServiceUnreachable
        expr: probe_success{instance="http://app:8000/ready"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Service readiness probe failing"
          description: "The /ready endpoint has not returned 200 for more than 1 minute."
